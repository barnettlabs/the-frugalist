# API base URL.
#
# In production the SPA and the API share a domain (Cloudflare routes /api/* to
# the API service), so the default relative path is correct and no value is
# needed. Set it only when running the API on a different host or port, which is
# the usual case locally.
VITE_API_URL=/api
